[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    unit: Unit tests for individual components with external dependencies mocked.
    integration: Integration tests for interactions between multiple components.
    live: Tests that call the real model and require Ollama to be running.